sudo dnf install vulkan vulkan-tools vulkan-validation-layers

Wayland:
    sudo dnf install wayland-devel libxkbcommon-devel libXrandr-devel libXinerama-devel libXcursor-devel libXi-devel mesa-libGL-devel

    if didn't help:
        cmake .. -DGLFW_USE_WAYLAND=OFF -DGLFW_USE_X11=ON